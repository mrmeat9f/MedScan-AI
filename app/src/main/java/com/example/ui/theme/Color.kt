package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Light, Clean Mint-Teal theme colors, precisely matching the attachment screenshot
val MintTeal = Color(0xFF0C9B8E)        // Core primary teal
val MintPrimary = Color(0xFF009688)     // App icon and primary buttons
val MintSubtitle = Color(0xFF00AA99)    // For sub-status tags and descriptions
val MintDarkText = Color(0xFF1C3B37)    // High contrast header text

val LightMint = Color(0xFFEFF7F6)       // Light green-grey for stats/cards
val LightBackground = Color(0xFFF4F8F7) // Beautiful minty background of the entire app
val LightSurface = Color(0xFFFFFFFF)    // Pure white for cards
val LightOnSurface = Color(0xFF1C3B37)  // Main text is deep teal-gray

val LavenderActive = Color(0xFFE8E0FC)  // Soft lavendar selected tab background
val LavenderText = Color(0xFF5B3C88)    // Rich violet for selected tab labels

// Custom semantic status colors for medicines (harmonized with the high density aesthetic)
val ColorExpired = Color(0xFFD32F2F)     // Soft medical red
val ColorExpiring = Color(0xFFFBC02D)    // Warning yellow-amber
val ColorValid = Color(0xFF00AA99)       // Positive teal-green
val ColorNeutral = Color(0xFF79747E)     // Slate-grey
